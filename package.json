{
  "name": "token-test",
  "version": "1.0.0",
  "description": "Design Token Pipeline",
  "scripts": {
    "build": "style-dictionary build --config config.json"
  },
  "devDependencies": {
    "style-dictionary": "^3.9.0"
  }
}
